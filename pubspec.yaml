name: animated_hint_textfield
description: Flutter package for animating textfield hint/label text, adding dynamic and eye-catching transitions to enhance user experience.
version: 1.0.2
homepage: https://github.com/deekshithx/animated_hint_textfield/
maintainer: Deekshith Shetty

environment:
  sdk: ">=3.1.1 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  animated_text_kit: ^4.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
